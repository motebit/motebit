<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 720 280" width="100%" preserveAspectRatio="xMidYMid meet" style="max-width: 100%; height: auto; font-family: ui-sans-serif, system-ui, sans-serif;">
  <title id="license-tiers-title">License tiers — protocol open, implementation source-available, state private</title>
  <desc id="license-tiers-desc">Motebit's license model has three tiers. The protocol floor (specs, types, crypto, SDK) is Apache-2.0 with an explicit patent grant — anyone can implement it, in any language, without permission. The reference implementation (runtime, engines, services, apps) is BSL 1.1, source-available today and converting to Apache-2.0 four years after each version's release. The accumulated state a relay generates by running — trust graph, federation routes, signed audit ledger — belongs to whoever runs the relay; it is never licensed, mirrored, or shared.</desc>

  <g style="font-family: inherit;">
    <!-- Tier 1: Apache-2.0 protocol floor -->
    <g>
      <rect x="80" y="40" width="560" height="56" fill="none"
            stroke="var(--color-fd-foreground, #1c1917)" stroke-width="1.25" />
      <text x="100" y="64" font-size="13" font-weight="600"
            fill="var(--color-fd-foreground, #1c1917)">Apache-2.0 — protocol floor</text>
      <text x="100" y="83" font-size="11"
            fill="var(--color-fd-muted-foreground, #78716c)">specs, types, crypto suite, SDK, hardware-attestation verifiers · permanent · patent grant</text>
    </g>

    <!-- Tier 2: BSL-1.1 reference implementation -->
    <g>
      <rect x="80" y="112" width="560" height="56" fill="none"
            stroke="var(--color-fd-foreground, #1c1917)" stroke-width="1"
            stroke-dasharray="6 4" />
      <text x="100" y="136" font-size="13" font-weight="500"
            fill="var(--color-fd-foreground, #1c1917)">BSL-1.1 — reference implementation</text>
      <text x="100" y="155" font-size="11"
            fill="var(--color-fd-muted-foreground, #78716c)">runtime, engines, services, apps · source-available now · → Apache-2.0 four years after release</text>
    </g>

    <!-- Tier 3: Private accumulated state — left-edge accent stripe so the visual weight survives standalone (no fill that can render as solid black) -->
    <g>
      <rect x="80" y="184" width="560" height="56" fill="none"
            stroke="var(--color-fd-foreground, #1c1917)" stroke-width="1" />
      <rect x="80" y="184" width="4" height="56"
            fill="var(--color-fd-primary, #0369a1)" />
      <text x="100" y="208" font-size="13" font-weight="500"
            fill="var(--color-fd-foreground, #1c1917)">Private — accumulated state</text>
      <text x="100" y="227" font-size="11"
            fill="var(--color-fd-muted-foreground, #78716c)">trust graph, federation routes, signed audit ledger · belongs to whoever runs the relay · never licensed</text>
    </g>
  </g>
</svg>
