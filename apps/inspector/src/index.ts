/**
 * @motebit/inspector — React + Vite single-agent inspector
 *
 * Features:
 * - State vector + history
 * - Memory graph visualizer
 * - Behavior cues debug overlay
 * - Goals, plans, conversations, devices
 * - Gradient + agent-trust + agent-graph
 * - Credentials + key succession + anchoring
 * - Event log + tool audit
 */

export { AdminApp } from "./AdminApp";
