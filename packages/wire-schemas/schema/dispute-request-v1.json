{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/motebit/motebit/main/packages/wire-schemas/schema/dispute-request-v1.json",
  "title": "DisputeRequest (v1)",
  "description": "Filing party's signed request to open a dispute on a completed task. Requires task_id + allocation_id + ≥1 evidence reference. Relays MUST NOT reject eligible disputes (§4.4). See spec/dispute-v1.md.",
  "type": "object",
  "properties": {
    "dispute_id": {
      "type": "string",
      "minLength": 1,
      "description": "UUIDv7 generated by the filing party. Stable through the entire dispute lifecycle."
    },
    "task_id": {
      "type": "string",
      "minLength": 1,
      "description": "References the disputed task. Foundation law §4.4: no economic binding (no `task_id` + `allocation_id`), no dispute."
    },
    "allocation_id": {
      "type": "string",
      "minLength": 1,
      "description": "References the task's BudgetAllocation. The dispute mechanism operates on the locked funds; without an allocation there's nothing to release/refund."
    },
    "filed_by": {
      "type": "string",
      "minLength": 1,
      "description": "Motebit identity of the filing party. MUST be a direct party to the referenced task (delegator OR executor) — third parties cannot file."
    },
    "respondent": {
      "type": "string",
      "minLength": 1,
      "description": "Motebit identity of the other task party. Receives notification + evidence window."
    },
    "category": {
      "type": "string",
      "enum": ["quality", "non_payment", "receipt_invalid", "unauthorized", "other"],
      "description": "Dispute category (§4.2). `quality` = work was unsatisfactory; `non_payment` = settlement didn't happen; `receipt_invalid` = receipt fails verification; `unauthorized` = work outside the scoped delegation; `other` = catch-all (description should explain)."
    },
    "description": {
      "type": "string",
      "description": "Human-readable explanation. Empty string is permitted (the evidence speaks for itself), but conventional usage includes a short factual narrative."
    },
    "evidence_refs": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1,
      "description": "References to signed artifacts supporting the dispute. At least one is required at filing time (foundation law §4.4) — disputes without evidence are noise."
    },
    "filed_at": {
      "type": "number",
      "description": "Unix timestamp in milliseconds when the filing party signed the request."
    },
    "suite": {
      "type": "string",
      "const": "motebit-jcs-ed25519-b64-v1",
      "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for dispute artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
    },
    "signature": {
      "type": "string",
      "minLength": 1,
      "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = the artifact minus `signature`. Signed by the filing party."
    }
  },
  "required": [
    "dispute_id",
    "task_id",
    "allocation_id",
    "filed_by",
    "respondent",
    "category",
    "description",
    "evidence_refs",
    "filed_at",
    "suite",
    "signature"
  ],
  "additionalProperties": false,
  "definitions": {
    "DisputeRequest": {
      "type": "object",
      "properties": {
        "dispute_id": {
          "type": "string",
          "minLength": 1,
          "description": "UUIDv7 generated by the filing party. Stable through the entire dispute lifecycle."
        },
        "task_id": {
          "type": "string",
          "minLength": 1,
          "description": "References the disputed task. Foundation law §4.4: no economic binding (no `task_id` + `allocation_id`), no dispute."
        },
        "allocation_id": {
          "type": "string",
          "minLength": 1,
          "description": "References the task's BudgetAllocation. The dispute mechanism operates on the locked funds; without an allocation there's nothing to release/refund."
        },
        "filed_by": {
          "type": "string",
          "minLength": 1,
          "description": "Motebit identity of the filing party. MUST be a direct party to the referenced task (delegator OR executor) — third parties cannot file."
        },
        "respondent": {
          "type": "string",
          "minLength": 1,
          "description": "Motebit identity of the other task party. Receives notification + evidence window."
        },
        "category": {
          "type": "string",
          "enum": ["quality", "non_payment", "receipt_invalid", "unauthorized", "other"],
          "description": "Dispute category (§4.2). `quality` = work was unsatisfactory; `non_payment` = settlement didn't happen; `receipt_invalid` = receipt fails verification; `unauthorized` = work outside the scoped delegation; `other` = catch-all (description should explain)."
        },
        "description": {
          "type": "string",
          "description": "Human-readable explanation. Empty string is permitted (the evidence speaks for itself), but conventional usage includes a short factual narrative."
        },
        "evidence_refs": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "minItems": 1,
          "description": "References to signed artifacts supporting the dispute. At least one is required at filing time (foundation law §4.4) — disputes without evidence are noise."
        },
        "filed_at": {
          "type": "number",
          "description": "Unix timestamp in milliseconds when the filing party signed the request."
        },
        "suite": {
          "type": "string",
          "const": "motebit-jcs-ed25519-b64-v1",
          "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for dispute artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
        },
        "signature": {
          "type": "string",
          "minLength": 1,
          "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = the artifact minus `signature`. Signed by the filing party."
        }
      },
      "required": [
        "dispute_id",
        "task_id",
        "allocation_id",
        "filed_by",
        "respondent",
        "category",
        "description",
        "evidence_refs",
        "filed_at",
        "suite",
        "signature"
      ],
      "additionalProperties": false
    }
  }
}
