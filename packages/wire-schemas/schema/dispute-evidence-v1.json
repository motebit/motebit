{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/motebit/motebit/main/packages/wire-schemas/schema/dispute-evidence-v1.json",
  "title": "DisputeEvidence (v1)",
  "description": "Cryptographically-verifiable evidence submitted in a dispute. Foundation law §5.4: both parties must have equal access; relay must not tamper or withhold. See spec/dispute-v1.md.",
  "type": "object",
  "properties": {
    "dispute_id": {
      "type": "string",
      "minLength": 1,
      "description": "References an open dispute. Evidence submitted to a non-existent or non-open dispute is rejected."
    },
    "submitted_by": {
      "type": "string",
      "minLength": 1,
      "description": "Motebit identity of the submitting party. MUST be one of the dispute's parties; third-party evidence routes through one of them."
    },
    "evidence_type": {
      "type": "string",
      "enum": [
        "execution_receipt",
        "credential",
        "anchor_proof",
        "settlement_proof",
        "execution_ledger",
        "attestation"
      ],
      "description": "Evidence type (§5.1). Each value names a category of cryptographically-verifiable artifact the adjudicator can validate against its own dedicated schema."
    },
    "evidence_data": {
      "type": "object",
      "additionalProperties": {},
      "description": "The signed artifact itself, as a JSON object. Inner shape validates against the dedicated wire schema for `evidence_type` (e.g. ExecutionReceiptSchema for `execution_receipt`). Per-entry validation deferred to those schemas."
    },
    "description": {
      "type": "string",
      "description": "Human-readable explanation of what this evidence demonstrates. Lets adjudicators quickly orient on the submitter's claim."
    },
    "submitted_at": {
      "type": "number",
      "description": "Unix timestamp in milliseconds when the evidence was signed."
    },
    "suite": {
      "type": "string",
      "const": "motebit-jcs-ed25519-b64-v1",
      "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for dispute artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
    },
    "signature": {
      "type": "string",
      "minLength": 1,
      "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = the artifact minus `signature`. Signed by the submitting party."
    }
  },
  "required": [
    "dispute_id",
    "submitted_by",
    "evidence_type",
    "evidence_data",
    "description",
    "submitted_at",
    "suite",
    "signature"
  ],
  "additionalProperties": false,
  "definitions": {
    "DisputeEvidence": {
      "type": "object",
      "properties": {
        "dispute_id": {
          "type": "string",
          "minLength": 1,
          "description": "References an open dispute. Evidence submitted to a non-existent or non-open dispute is rejected."
        },
        "submitted_by": {
          "type": "string",
          "minLength": 1,
          "description": "Motebit identity of the submitting party. MUST be one of the dispute's parties; third-party evidence routes through one of them."
        },
        "evidence_type": {
          "type": "string",
          "enum": [
            "execution_receipt",
            "credential",
            "anchor_proof",
            "settlement_proof",
            "execution_ledger",
            "attestation"
          ],
          "description": "Evidence type (§5.1). Each value names a category of cryptographically-verifiable artifact the adjudicator can validate against its own dedicated schema."
        },
        "evidence_data": {
          "type": "object",
          "additionalProperties": {},
          "description": "The signed artifact itself, as a JSON object. Inner shape validates against the dedicated wire schema for `evidence_type` (e.g. ExecutionReceiptSchema for `execution_receipt`). Per-entry validation deferred to those schemas."
        },
        "description": {
          "type": "string",
          "description": "Human-readable explanation of what this evidence demonstrates. Lets adjudicators quickly orient on the submitter's claim."
        },
        "submitted_at": {
          "type": "number",
          "description": "Unix timestamp in milliseconds when the evidence was signed."
        },
        "suite": {
          "type": "string",
          "const": "motebit-jcs-ed25519-b64-v1",
          "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for dispute artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
        },
        "signature": {
          "type": "string",
          "minLength": 1,
          "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = the artifact minus `signature`. Signed by the submitting party."
        }
      },
      "required": [
        "dispute_id",
        "submitted_by",
        "evidence_type",
        "evidence_data",
        "description",
        "submitted_at",
        "suite",
        "signature"
      ],
      "additionalProperties": false
    }
  }
}
