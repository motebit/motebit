{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/motebit/motebit/main/packages/wire-schemas/schema/memory-deleted-payload-v1.json",
  "title": "MemoryDeletedPayload (v1)",
  "description": "Payload of a `memory_deleted` event — tombstones a node; the original memory_formed event persists. See spec/memory-delta-v1.md §5.4.",
  "type": "object",
  "properties": {
    "node_id": {
      "type": "string",
      "minLength": 1,
      "description": "UUID of the deleted node. Tombstones it in-log."
    }
  },
  "required": ["node_id"],
  "additionalProperties": true,
  "definitions": {
    "MemoryDeletedPayload": {
      "type": "object",
      "properties": {
        "node_id": {
          "type": "string",
          "minLength": 1,
          "description": "UUID of the deleted node. Tombstones it in-log."
        }
      },
      "required": ["node_id"],
      "additionalProperties": true
    }
  }
}
