/**
 * Reflection engine — re-exported from @motebit/reflection.
 *
 * The adaptive intelligence loop now lives in its own package.
 * This shim preserves backward compatibility for internal runtime imports.
 */

export { type ReflectionDeps, performReflection, runReflectionSafe } from "@motebit/reflection";
