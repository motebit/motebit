# Evidence provenance — verifiable locality, from signatures to evidence

The `VerificationVerdict`'s `evidenceBasis: EvidenceRef[]` was an emerging axis: a list of `{ kind, ref }` POINTERS naming what a verdict used, but not independently re-checkable. This arc (co-designed with agency.computer, 2026-06, the second verify-family consumer — the same consumer-forces-need / producer-forces-shape loop as the [VerificationVerdict reshape](verify-family-fail-closed.md)) makes that pointer **resolve to a re-verifiable provenance**, extending the system's deepest property — _verify the claim without trusting the claimant_ — from a SIGNATURE ("is this artifact authentic?") to EVIDENCE ("is this claim backed by a primary record?").

## The law

`verifyEvidenceProvenance(bytes, provenance, { resolveProjection? })` (`@motebit/crypto`, pure, I/O-free): **the named `span` is an exact substring of `projection(bytes)`, where the bytes content-address to `digest`.** It re-verifies PRESENCE — never TRUTH, with no oracle. The bytes either contain the span or they don't. "Model proposes, code disposes": a fabricated figure cannot be placed into the record, because the verifier only accepts an exact substring of content-addressed bytes — the [runtime-invariants-over-prompt-rules](runtime-invariants-over-prompt-rules.md) pattern applied to evidence.

The shape (`@motebit/protocol`, additive, back-compat by absence — a producer that doesn't retrieve emits bare `{ kind, ref }` as before):

```
EvidenceRef       = { kind, ref, provenance? }
EvidenceProvenance = { digest: { algorithm, value }, projection?, projectionClass?, span, locator?, binding? }
```

## The boundary (load-bearing)

motebit owns the **shape** and the **re-check law**; NEVER the venue authority. Deciding what counts as a primary record, the document→text projection, and domain identity semantics (e.g. SEC/CIK) are **app-layer**, the consumer's. The subject is re-verifiable _presence_, not _truth_.

## The three guardrails (and the keystone)

1. **Hash-agility, not a baked-in algorithm.** `digest` is `{ algorithm, value }` (a `DigestAlgorithm` role — a content digest is hashed, not signed, so it rides its own role, not `SuiteId`). `sha-256` today; a new hash is a registry append, not a wire break ([agility-as-role](agility-as-role.md)).
2. **Domain-blind identity binding.** `binding?` references an opaque resolved identity (a `motebit_id` or a domain token the consumer resolves) — it is CARRIED, not verified by the law. Who is a valid issuer is app-layer.
3. **Byte-exact reproducibility — the make-or-break.** The `digest` is over the RAW, independently-obtainable bytes (a third party fetches the same primary source). The `span` is re-checkable iff EITHER `projection` is absent (located over the raw bytes directly — re-verifiable _by construction_, the **default**) OR the named recipe is spec'd to byte-determinism: a third party reimplements it from its spec to byte identity, proven by a conformance fixture that pits TWO INDEPENDENT implementations against each other (one impl checked against itself is the nominal-recipe trap). Same discipline as JCS canonicalization — both sides must produce byte-identical input or it fails mysteriously.

**Keystone — projection is an injected seam.** Because the projection recipe is app-owned (guardrail 1's twin), `verifyEvidenceProvenance` takes it as an INJECTED function — the same shape as `verifyStandingDelegation`'s `isRevoked`. Projection absent → motebit does it fully (raw bytes). Projection present + resolver → apply, then check. Projection present + no resolver → **fail closed** (`projection_unresolved`). This keeps the verifier pure and domain-blind; the recipe implementation never enters motebit's tree. `locator?` is advisory (the law is substring presence), never a second thing a re-verifier must reproduce.

The injected resolver is assumed **total** for any recipe it accepts: a resolver that THROWS propagates the exception — a resolver fault is a caller bug, NOT an evidence verdict, and is never swallowed into a false `present:false` (which would let a broken recipe masquerade as "evidence absent" and hide the bug). The signal for "I cannot resolve this recipe" is to OMIT the resolver for it and let the no-resolver path fail closed (`projection_unresolved`), never a throwing resolver — i.e. inject a resolver only for the recipes the consumer owns, and let every other recipe fall through. (Contract clarified from agency.computer's adoption, 2026-06 — the consumer-forces-shape loop hardening the prose against the real package.)

## Two projection conformance classes — keeping §7 binary (agency.computer co-design, 2026-06)

§7's whole value is that it is **binary**: a recipe either is independently reimplementable from prose or it is not a real protocol artifact. PDF→text breaks this — a PDF is a graphics format, "the text" (reading order, columns, tables) is a genuine INFERENCE, and `pdftotext`/`pdf.js`/`pdfminer`/`mupdf` disagree byte-for-byte; there is no honest §7 recipe for it. If such a recipe shipped under the same `projection` umbrella, §7 would go soft ("usually real"), and a "verified" PDF span would silently re-verify only against the producer's exact library — the quiet falsehood §7 exists to forbid.

The fix is NOT to keep PDF out; it is to make the weaker assurance **visibly weaker, per claim**. `projectionClass` (a closed `ProjectionClass` registry, on-wire) declares HOW a present projection is re-verified and BY WHOM:

- **`spec-reproducible`** (§7.1, the strong rung, the **default**) — reimplementable from the published spec to byte identity, proven by a TWO-independent-implementation fixture. `agency.html-text.v1` is the exemplar.
- **`tool-pinned`** (§7-tool, the lesser rung) — byte-reproducible only by running the recipe's **content-addressed, world-obtainable, version-pinned tool** (a build digest, reproducible-build preferred) against a committed fixture (ONE implementation, the pinned one). A real but lesser assurance; never §7.1.

Four load-bearing decisions, all derivable from the existing doctrine:

1. **On-wire, as a closed enum — not a naming convention.** The class is the metadata that says how re-checkable the span is; a consumer must see it at the point of the claim, fail-closed, without a catalog lookup (else a tool-pinned span is silently read as §7-grade — the exact undetected-until-a-stranger-disagrees failure). Encoding it in the recipe id would force motebit to PARSE the app-owned id namespace — a domain-authority leak (the id stays opaque per guardrail 1's twin). On-wire also makes the weak class pay a **visibility cost**, which is what stops it being chosen out of laziness.
2. **Absent ⇒ `spec-reproducible`.** The strong rung is the default, so the weaker class is **opt-in** and can never be claimed by omission; every span emitted before the class existed keeps the strong reading (back-compat). A `tool-pinned` recipe that omits the field over-claims §7.1 and is caught by the missing two-impl fixture.
3. **The tool digest is OFF-wire.** It lives in the app-owned recipe spec, already bound by the immutable-recipe-id rule (a new tool digest is a new recipe id, §7-tool.3). Carrying it per-span would be redundant and would pull an app-owned implementation detail onto motebit's protocol surface.
4. **World-obtainable tool is the honesty line.** A content-addressed digest of a producer-PRIVATE build is not a lesser class — it IS the §6 "agency-re-verifiable only" footgun. `tool-pinned` is honest only because anyone can obtain and re-run the pinned tool; reproducible-build recovers byte-reproducibility one level down (reproduce the binary from pinned source).

**Boundary preserved.** motebit owns the class VOCABULARY and the conformance obligations (shape + law); it never owns the recipe catalog (which tool, which digest — app-owned) and never adjudicates whether `tool-pinned` is good enough for a given use. Like `binding` and `locator`, the class is CARRIED but NOT verified by the law — `verifyEvidenceProvenance` applies the injected recipe regardless; the class is the assurance level the CONSUMER policies on. The honest ladder is exactly these two rungs plus the null (a projection nobody can re-run collapses to the bare `EvidenceRef` pointer — there is no "advisory projection" third rung). Lighter closed-union treatment (`ALL_PROJECTION_CLASSES` + `isProjectionClass`, mirroring `DigestAlgorithm`), NOT the registered-registry ceremony — crystallize the eight-artifact set when a third class (e.g. a TEE-attested tool run) or a second independent consumer arrives.

## Status

**Shipped:** the additive `EvidenceProvenance`/`EvidenceRef` vocabulary in `@motebit/protocol` (graduated from `@motebit/crypto`'s free `{kind,ref}`), `verifyEvidenceProvenance` with the injected projection seam in `@motebit/crypto`, the hostile corpus (span-absent, digest-mismatch, projection-unresolved-fail-closed, raw-byte-happy, projection-applied, projection-divergence, binding-carried-not-verified), and the law re-exported through `@motebit/verifier` so a consumer pinning the aggregator re-checks evidence from the same surface as the verdict family (the agency-proof-integration contract — consume the verifier, never fork it; not reaching past it into `@motebit/crypto`).

**Shipped — the make-or-break guardrail, now empirically proven.** agency.computer published `agency.html-text.v1` as a frozen, **world-public**, citable spec — a scoped public repo carrying only the recipe (`github.com/agency-computer/html-text-spec` @ `01b475be`; raw spec + fixture return 200 unauthenticated, verified). World-public reachability is not incidental: it IS the property — the byte-determinism guardrail only means anything if ANY third party (not just agency's partner) can re-fetch the recipe and re-derive a span. motebit then stood up the **real cross-implementation conformance case** (`packages/crypto/src/__tests__/evidence-provenance-conformance.test.ts`): a second implementation written from §2 of the spec ALONE — their reference `projection.ts` deliberately NOT read — reproduces all 7 vectors **byte-for-byte**, including the single-pass-decode canary (`a&amp;lt;b` → `a&lt;b`). So the byte-exact-reproducibility crux (guardrail 3) is no longer asserted, it is demonstrated: the recipe is a genuine public protocol artifact, not an agency-private convenience. **Boundary preserved:** the conformance impl is a TEST-only independent reimplementation (motebit acting as the second implementer the guardrail requires); the shipped `verifyEvidenceProvenance` still owns no recipe (domain-blind, injected resolver), and v1's immutability (`§5` — any output change is a new id) makes the vendored fixture a frozen, zero-maintenance proof.

**Shipped — the wire spec.** [`spec/evidence-provenance-v1.md`](../../spec/evidence-provenance-v1.md) (`motebit/evidence-provenance@1.0`) formalizes the `EvidenceProvenance` foundation-law wire shape (with `EvidenceRef` as its carrier, `DigestRef` nested), the re-check law + structured result, the injected-projection seam + resolver-totality contract, the three guardrails, and — §7 — the **byte-determinism conformance requirement** a projection recipe MUST meet (world-public + content-addressed spec, immutable id, two independent implementations against a committed fixture), citing `agency.html-text.v1 @ 01b475be` as the worked exemplar. Backed by a runtime zod schema + committed JSON Schema (`@motebit/wire-schemas` → `spec/schemas/evidence-provenance-v1.json`) so a non-TS producer validates the shape at its own boundary.

**Closed in PRODUCTION (2026-06), both sides.** agency.computer is the live producer: their research + monitor engine retrieves the primary record (EDGAR filings + any cited primary venue — IR pages, press releases, government disclosures — SSRF-hardened) and POPULATES `EvidenceProvenance` on the signed brief's `evidenceBasis` — digest over the raw bytes, `projection: agency.html-text.v1` (or absent for raw-byte spans), `binding: sec:cik:<cik>` only when the document is the resolved issuer's OWN filing (else carried-not-verified-absent — the domain-blind contract working exactly as §6 guardrail 2 specifies). Proven end-to-end on a real Fed/BLS release (read the record, located the verbatim span, honest `retrieved` rung). So a stranger re-fetches the primary record and re-checks the cited span with zero trust in agency's index and zero motebit types — verifiable-locality from signatures to EVIDENCE, standing in production on both ends (producer + verifier), against artifacts anyone can re-fetch and re-derive. The citation is bidirectional: the recipe spec links the protocol spec and back.

**Shipped — motebit is now ALSO a producer (dogfooding, 2026-06).** motebit's own research-agent `"web"` citations carry re-verifiable `Citation.provenance` down to the retrieved source, on BOTH paths. The chain is additive (back-compat by absence): `read_url` attests `source_digest` over the RAW fetched bytes (+ `source_projection` on the recipe path), threaded into the signed `ExecutionReceipt` and copied by the research engine into `Citation.provenance`. **Raw-byte path** (`text/*`): `data` is the served bytes verbatim, projection absent — the span is located over the raw bytes directly. **Recipe path** (HTML): rather than publish a near-identical `motebit.read-url-text.v1`, motebit ADOPTS the world-public `agency.html-text.v1` — the Metabolic Principle (a deterministic HTML→text transform is solved glucose, not a motebit enzyme; one resolver re-checks BOTH parties' HTML citations — the protocol working across parties, not just asserted). The production recipe impl (`projectAgencyHtmlTextV1`, exported from `@motebit/tools` as the producer transform AND an injectable resolver) is conformance-tested byte-for-byte against the published fixture; the independent impl in `@motebit/crypto` remains the §7 byte-determinism guard. **Boundary preserved:** the recipe lives in the PRODUCER (`@motebit/tools`), never in the domain-blind verifier. A round-trip e2e proves a stranger re-verifies a real research citation on both paths, that the recipe path fails closed without the resolver (`projection_unresolved`), and that a fabricated span fails closed (`span_absent`). Deferred-with-trigger: JSON output stays projection-absent until a JSON recipe lands; the browser `proxyUrl` path (edge runtime, not a signed-citation producer) carries no provenance.

**Shipped — the two projection conformance classes (2026-06).** `projectionClass` (`ProjectionClass` closed registry in `@motebit/protocol` — `ALL_PROJECTION_CLASSES` + `isProjectionClass`, lighter than the registered-registry ceremony, mirroring `DigestAlgorithm`) added to the `EvidenceProvenance` wire shape, the zod schema + committed JSON Schema (parity-checked), and `spec/evidence-provenance-v1.md` (§7 split into §7.1 `spec-reproducible` + §7-tool `tool-pinned`). The law (`verifyEvidenceProvenance`) is UNCHANGED — the class is carried-but-law-advisory, like `binding`/`locator`; it is the assurance level the consumer policies on. Settled with agency.computer's PE before any agency code (the class shapes what they build): the PDF case forced the second class because PDF→text is a genuine inference with no canonical output, and shipping it under §7 would soften §7 to nothing. NOT YET a producer: no `tool-pinned` recipe exists in-tree (the PDF recipe is agency's, off-wire/app-owned); motebit ships the class vocabulary + conformance obligations only. Trigger for the first `tool-pinned` recipe + its conformance fixture: agency (or motebit) needs to cite a PDF primary record.

**Shipped — cross-language conformance (2026-06).** The "verify in any language, no relay, offline" claim is the moat, and it now covers evidence-provenance, not just receipts. A language-neutral corpus ([`spec/conformance/evidence-provenance/corpus.json`](../../spec/conformance/evidence-provenance/corpus.json), six vectors over both paths × `present`/`digest_mismatch`/`projection_unresolved`/`span_absent`) is verified by BOTH `@motebit/crypto`'s `verifyEvidenceProvenance` and a **stdlib-only Python reference** ([`examples/python-receipt-verifier/verify_evidence_provenance.py`](../../examples/python-receipt-verifier/verify_evidence_provenance.py) — evidence-provenance needs no signing library, just sha-256 + substring), with `check-evidence-provenance-conformance` (the evidence-axis sibling of `check-receipt-conformance`) asserting both agree with each other and with `expected` (`REQUIRE_PYTHON=1` in CI). The recipe path makes this a THIRD independent `agency.html-text.v1` implementation — in a different language — proving the §7 byte-determinism crux survives a language boundary, not just two TS impls. So an external party who only has Python can now re-verify a motebit (or agency) citation down to the primary record; the protocol is real, not a TypeScript habit.
