# Production dependency triage — 2026-09-13

The release gate for dependency advisories is `pnpm audit --prod --audit-level=high` in CI (`.github/workflows/ci.yml`), raised from `critical` on 2026-09-13. A HIGH or CRITICAL advisory fails CI unless it is accepted explicitly in `package.json` → `pnpm.auditConfig.ignoreCves`, and every accepted CVE has a row below with a reachability argument and a **lift trigger**. A bare count threshold is not a policy; this ledger is.

Baseline before this pass (commit `2ff5740c`): 54 advisories — 23 high, 29 moderate, 2 low. After: 0 high not accepted, 2 high accepted (no fix exists), 4 moderate visible-and-documented, 1 low.

## Fixed by override or bump

Overrides are upper-bounded (`>=patched <next-major`) per repo convention; each is a decision record, and a Dependabot major bump that collides with one is a deliberate paired change, never a merge.

| Module            | Was    | Now       | Entered via                                             | Reachability                                                                                                                                                   | Fix                                                                                                                                |
| ----------------- | ------ | --------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `axios`           | 1.13.6 | ≥1.18.0   | `@coinbase/x402` → `@coinbase/cdp-sdk` (relay)          | Prod relay, x402 facilitator calls. Nine high advisories (prototype-pollution gadgets, proxy credential leak, ReDoS).                                          | override `axios: >=1.18.0 <2.0.0`                                                                                                  |
| `fast-uri`        | 3.1.5  | ≥3.1.6 <4 | `@modelcontextprotocol/sdk` → `ajv`; `@x402/extensions` | Every MCP surface (schema validation). Host-confusion / SSRF-class parsing.                                                                                    | override `fast-uri@>=3.0.0 <3.1.6: >=3.1.6 <4.0.0` (4.x carries its own open advisories; stay on the patched 3.x that ajv expects) |
| `form-data`       | 4.0.5  | ≥4.0.6    | `@coinbase/cdp-sdk` (relay)                             | Prod relay. CRLF injection in multipart.                                                                                                                       | override `form-data@>=4.0.0 <4.0.6: >=4.0.6 <5.0.0`                                                                                |
| `ws`              | 8.17.1 | ≥8.21.0   | `@x402/extensions` (relay, cli)                         | Prod relay + CLI (direct deps were already ^8.21.3; this closes the transitive copy). Memory-exhaustion DoS.                                                   | override `ws@>=8.0.0 <8.21.0: >=8.21.0 <9.0.0`                                                                                     |
| `sharp`           | 0.32.6 | ≥0.35.4   | `@xenova/transformers` (memory-graph)                   | Desktop/CLI runtime; motebit uses transformers for **text** embeddings only, the image pipeline is never invoked, but libvips/libheif CVEs ship in the binary. | override `sharp: >=0.35.4 <0.36.0`                                                                                                 |
| `hono`            | 4.13.2 | ≥4.13.5   | direct (relay, cli, browser-sandbox)                    | Prod relay HTTP surface. `parseBody` nesting DoS, fragment-in-query differential.                                                                              | direct bump `^4.13.5` + override floor                                                                                             |
| `js-yaml`         | 4.3.1  | ≥4.3.2    | expo tooling (mobile)                                   | Build-time.                                                                                                                                                    | override `js-yaml@>=4.0.0 <4.3.2: >=4.3.2 <5.0.0` (5.x is a different major)                                                       |
| `brace-expansion` | 5.0.8  | ≥5.0.9    | `vscode-languageclient` → `minimatch` (vscode ext)      | Extension host.                                                                                                                                                | override `brace-expansion@>=4.0.0 <5.0.9: >=5.0.9 <6.0.0`                                                                          |
| `protobufjs`      | 7.6.4  | ≥7.6.5    | `onnxruntime-web` → `onnx-proto` (memory-graph)         | Model loading; `.proto` option parse loop.                                                                                                                     | override floor bumped                                                                                                              |
| `qs`              | 6.15.3 | ≥6.16.0   | `@modelcontextprotocol/sdk` → `express`                 | MCP HTTP surfaces.                                                                                                                                             | override `qs@>=2.2.5 <6.16.0: >=6.16.0 <7.0.0`                                                                                     |
| `uuid` (11.x)     | 11.1.0 | ≥11.1.1   | `@solana/web3.js` chain                                 | v3/v5/v6 `buf` bounds bug; motebit uses v4.                                                                                                                    | override `uuid@>=11.0.0 <11.1.1: >=11.1.1 <12.0.0`                                                                                 |

## Accepted — no fix exists (in `ignoreCves`)

| CVE                            | Module                       | Entered via                                         | Why accepted                                                                                                                                                                                                                                                                  | Lift trigger                                                                                                                       |
| ------------------------------ | ---------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| CVE-2025-3194                  | `bigint-buffer` 1.1.5 (high) | `@solana/spl-token` → `@solana/buffer-layout-utils` | `toBigIntLE` buffer overflow in the native addon. Inputs are SPL token-account layouts read from the operator's configured RPC endpoint, never from an untrusted caller; the JS fallback is used where the addon does not build. No patched release exists (latest is 1.1.5). | A `bigint-buffer` release >1.1.5, or `@solana/spl-token` dropping `buffer-layout-utils`. Re-check monthly via `pnpm audit --prod`. |
| CVE-2025-71329, CVE-2025-71330 | `image-size` 1.2.1 (high)    | `@expo/metro-runtime`, `react-native` (mobile)      | ICNS/JXL/HEIF parser infinite loops. Reached only by the Metro bundler over the app's own asset files at build time; not a runtime input path. Latest release (2.0.2) is still in the vulnerable range.                                                                       | An `image-size` release >2.0.2 adopted by Expo SDK; the Expo SDK 57 arc (#416).                                                    |

## Visible, documented, below the gate (moderate / low)

| Advisory            | Module                       | Why not forced                                                                                                                                                      | Lift trigger                                            |
| ------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| GHSA-vcc3-ghjq-m6fr | `decode-uri-component` 0.2.2 | Patched line (≥0.5.0) is ESM-only; forcing it under `query-string@7` inside Metro breaks the mobile bundle. Build/dev tooling input.                                | Expo SDK 57 arc.                                        |
| GHSA-528h-pc64-c93x | `stream-json` 1.9.1          | Patched line is a major (3.5.0) under `@solana/web3.js`; O(depth²) filter on nested JSON from the operator's RPC, not a public input.                               | `@solana/web3.js` bump that carries `stream-json` ≥3.5. |
| GHSA-w5hq-g745-h8pq | `uuid` 7.0.3 / 8.3.2         | Expo and web3.js majors; the bug is in v3/v5/v6 with a caller-supplied `buf`, motebit's code paths use v4. 11.x line fixed above.                                   | Same arcs as above.                                     |
| GHSA-g7r4-m6w7-qqqr | `esbuild` 0.27.7 (low)       | Windows dev-server arbitrary read; `apps/docs` build tooling via fumadocs/vite. The `<0.28.0` override cap (2026-03-10) is a compatibility decision with vite/tsup. | Lift the cap when vite/tsup declare 0.28 support.       |

## Process

- Re-run `pnpm audit --prod` before each release; a new HIGH fails CI by construction.
- Adding to `ignoreCves` requires a row here with a reachability argument and a lift trigger in the same commit.
- Dependabot majors colliding with an override cap are a paired change (lift cap + bump + revalidate), per `.github/dependabot.yml` comments.
