# motebit/dispute@1.0

**Status:** Draft  
**Authors:** Daniel Hakim  
**Created:** 2026-04-11

## 1. Purpose

A dispute is a challenge to the outcome of a delegation. The delegator claims the work was inadequate. The worker claims it was not paid. Either party claims the receipt or settlement is invalid. This spec defines how disputes are raised, evidenced, adjudicated, and resolved — turning the `disputed` state in `BudgetAllocation` (delegation@1.0 §6.1) into a concrete resolution process with cryptographic evidence, bounded timelines, and defined fund handling.

Two adjudication modes exist. **Single-relay** disputes are adjudicated by the relay operator when both parties and the relay agree on jurisdiction. **Federation** disputes use peer consensus for cross-relay tasks or when the relay itself is a defendant — because no entity may judge its own case.

## 2. Design Principles

**Evidence-first.** Every dispute claim must be backed by cryptographically verifiable evidence. A bare assertion ("the work was bad") is not a dispute — it is a complaint. Disputes require signed artifacts: receipts, credentials, settlement proofs, ledger entries, or third-party attestations.

**Non-repudiable.** Every message in the dispute lifecycle is signed by its author and timestamped. No party can deny having filed, responded to, or resolved a dispute. The audit trail is self-verifiable without relay cooperation.

**Bounded.** Every state has a deadline. Silence resolves to the status quo — the original settlement stands. An unbounded dispute is a denial-of-service vector against the counterparty's locked funds.

**Sybil-resistant.** Frivolous disputes carry cost. Overturned disputes produce negative trust signals. A minimum trust threshold prevents fresh identities from weaponizing the dispute system.

## 3. Dispute Lifecycle

### 3.1 State Machine

```
opened → evidence → arbitration → resolved(upheld|overturned|split)
                                    → appealed → final(upheld|overturned|split)
opened → expired
```

### 3.2 State Definitions

| State         | Meaning                                                            |
| ------------- | ------------------------------------------------------------------ |
| `opened`      | Filed by a task party. Funds locked. Evidence window begins.       |
| `evidence`    | Both parties submitting evidence. Clock running.                   |
| `arbitration` | Evidence window closed. Adjudicator(s) reviewing.                  |
| `resolved`    | Adjudicator issued resolution: `upheld`, `overturned`, or `split`. |
| `appealed`    | Losing party filed appeal within window. Second review begins.     |
| `final`       | Appeal resolved or appeal window expired. Terminal.                |
| `expired`     | Dispute did not transition to `evidence` within 1 hour. Terminal.  |

### 3.3 Foundation Law

- Terminal states (`final`, `expired`) are irreversible.
- A dispute in `opened` must transition to `evidence` within 1 hour or expire.
- Resolution must specify exactly one of `upheld`, `overturned`, or `split`.
- `resolved` transitions to `final` automatically if no appeal is filed within the appeal window.

## 4. Dispute Initiation

### 4.1 Who Can Dispute

Either the delegator or the worker on a completed task. The filing party must be a direct party to the task — not a sub-delegate, not a credential issuer, not a federation peer.

### 4.2 Dispute Request

```
DisputeRequest {
  dispute_id:       string          // UUID v7, generated by filing party
  task_id:          string          // Must reference an existing task
  allocation_id:    string          // Must reference the task's BudgetAllocation
  filed_by:         string          // MotebitId of the filing party
  respondent:       string          // MotebitId of the other task party
  category:         string          // "quality" | "non_payment" | "receipt_invalid" | "unauthorized" | "other"
  description:      string          // Human-readable explanation
  evidence_refs:    string[]        // At least one reference to a signed artifact
  filed_at:         number          // Unix ms
  signature:        string          // Ed25519 over canonical JSON of all fields except signature
}
```

### 4.3 Eligibility

- `filed_by` must be the delegator or worker on `task_id`.
- The task must be in a terminal state (`completed`, `failed`, `denied`).
- Filing must occur within the dispute window after settlement.
- Filing party's trust level must meet the minimum threshold.
- `evidence_refs` must contain at least one entry.

### 4.4 Foundation Law

- `task_id` and `allocation_id` are required. A dispute without economic binding is rejected.
- Filing party must be a direct party to the referenced task.
- At least one evidence reference is required at filing time.
- A relay must not reject an eligible dispute. Suppressing a valid dispute is a protocol violation.

### 4.5 Convention

- Dispute window: 24 hours after settlement.
- Minimum trust threshold: `FirstContact` (trust_level >= 0.05).

## 5. Evidence

### 5.1 Evidence Types

| Type                | Description                                                 |
| ------------------- | ----------------------------------------------------------- |
| `execution_receipt` | Signed ExecutionReceipt proving task outcome                |
| `credential`        | W3C VC 2.0 credential relevant to the dispute               |
| `anchor_proof`      | Onchain credential or receipt anchor hash                   |
| `settlement_proof`  | Payment proof from settlement rail                          |
| `execution_ledger`  | Timeline entries from execution-ledger@1.0                  |
| `attestation`       | Third-party signed statement (e.g., from a federation peer) |

### 5.2 Dispute Evidence

```
DisputeEvidence {
  dispute_id:       string          // Must reference an open dispute
  submitted_by:     string          // MotebitId of the submitting party
  evidence_type:    string          // One of the types in §5.1
  evidence_data:    object          // The signed artifact itself
  description:      string          // What this evidence demonstrates
  submitted_at:     number          // Unix ms
  signature:        string          // Ed25519 over canonical JSON of all fields except signature
}
```

### 5.3 Evidence Window

The evidence window begins when the dispute transitions to `evidence` and runs for a bounded period. Both parties have equal opportunity to submit evidence. The relay must deliver evidence submissions to both parties.

### 5.4 Foundation Law

- Evidence must be cryptographically verifiable. Unsigned or tampered evidence is rejected.
- Both parties must have equal access to the evidence window. A relay must not accept evidence from one party while blocking the other.
- The relay must not tamper with or withhold submitted evidence.

### 5.5 Convention

- Evidence window: 48 hours from dispute opening.
- Maximum 10 evidence submissions per party per dispute.

## 6. Adjudication

### 6.1 Single-Relay Adjudication

For tasks that executed within a single relay's jurisdiction, the relay operator (or automated policy engine) reviews the evidence and produces a signed resolution. The relay is the default adjudicator when it is not a party to the dispute.

### 6.2 Federation Adjudication

Federation adjudication is required in two cases:

1. **Cross-relay disputes.** When the task routed through multiple relays, no single relay has complete jurisdiction.
2. **Relay-as-defendant.** When the dispute alleges relay misconduct (fund withholding, evidence suppression, settlement failure), the relay must not judge its own case.

Federation adjudication requires a minimum 3-peer quorum from the federation graph. Each peer reviews the evidence independently and votes: `upheld`, `overturned`, or `split`. Majority wins. Ties resolve to `split`.

### 6.3 Relay-as-Defendant

When the agent disputes the relay itself, the agent's onchain proofs — credential anchors (credential-anchor@1.0) and settlement anchors (settlement@1.0 §7) — serve as evidence the relay cannot censor, because they exist independently on the public ledger. Adjudication MUST go to federation. If no federation peers are available, the dispute is recorded on the agent's local ledger as unresolvable, but the agent's onchain evidence exists independently and survives relay death.

### 6.4 Dispute Resolution

```
DisputeResolution {
  dispute_id:          string          // Must reference the dispute
  resolution:          string          // "upheld" | "overturned" | "split"
  rationale:           string          // Signed explanation of the decision
  fund_action:         string          // "release_to_worker" | "refund_to_delegator" | "split"
  split_ratio:         number          // [0, 1], worker's portion. 1.0 = all to worker.
  adjudicator:         string          // MotebitId or relay ID of the adjudicating entity
  adjudicator_votes:   AdjudicatorVote[]  // For federation — empty for single-relay
  resolved_at:         number          // Unix ms
  signature:           string          // Ed25519 over canonical JSON of all fields except signature
}

AdjudicatorVote {
  peer_id:     string          // Federation peer MotebitId
  vote:        string          // "upheld" | "overturned" | "split"
  rationale:   string          // Per-peer explanation
  signature:   string          // Ed25519 by the voting peer
}
```

### 6.5 Foundation Law

- Resolution must include a signed rationale. A bare verdict without explanation is rejected.
- Federation resolution must include individual `AdjudicatorVote` entries from each participating peer. Aggregated-only verdicts are rejected.
- A relay must not self-adjudicate when it is the defendant. Violation is a federation-level trust event.

### 6.6 Convention

- Adjudication deadline: 72 hours from evidence window close.
- Timeout without resolution defaults to `split` with `split_ratio: 0.5`.

## 7. Fund Handling

### 7.1 Lock on Dispute

When a dispute is opened, the referenced `BudgetAllocation` transitions to `disputed` (delegation@1.0 §6.1). Funds are locked for the duration of the dispute lifecycle — from `opened` through `resolved` (or `final`, if appealed).

### 7.2 Resolution Actions

| Resolution   | Fund Action              | Effect                                                                   |
| ------------ | ------------------------ | ------------------------------------------------------------------------ |
| `upheld`     | Per filing party's claim | Funds move as the filing party requested                                 |
| `overturned` | Original settlement      | The pre-dispute settlement stands                                        |
| `split`      | Divide by `split_ratio`  | Worker receives `locked * split_ratio`, delegator receives the remainder |

### 7.3 Foundation Law

- Funds must remain locked from `opened` through resolution. No partial release during the dispute.
- Fund release must exactly match the `fund_action` in the signed `DisputeResolution`.
- Split arithmetic uses integer micro-units. `worker_amount = floor(locked * split_ratio)`, `delegator_amount = locked - worker_amount`. No rounding error. No floating-point.

### 7.4 Convention

- Platform fee is not re-extracted on dispute resolution. The fee was already computed at original settlement; dispute resolution redistributes the net amount only.

## 8. Appeal

### 8.1 Appeal Window

Either party may appeal a `resolved` dispute once. The appeal window opens at `resolved_at` and runs for a bounded period. If no appeal is filed, the dispute transitions to `final` automatically.

### 8.2 Dispute Appeal

```
DisputeAppeal {
  dispute_id:            string          // Must reference a resolved dispute
  appealed_by:           string          // MotebitId of the appealing party
  reason:                string          // Why the resolution is incorrect
  additional_evidence:   string[]        // Optional: new evidence refs
  appealed_at:           number          // Unix ms
  signature:             string          // Ed25519 over canonical JSON of all fields except signature
}
```

### 8.3 Appeal Adjudication

Single-relay appeals go to operator review with the additional evidence. Federation appeals trigger a second vote round with the new evidence included.

### 8.4 Foundation Law

- One appeal per dispute. The `final` state after appeal is terminal — no further recourse within the protocol.
- New evidence may be submitted with the appeal. The appeal adjudicator considers all evidence (original + new).
- Final resolution is irreversible. Fund release after `final` cannot be re-disputed.

### 8.5 Convention

- Appeal window: 24 hours after `resolved_at`.

## 9. Sybil Defense

### 9.1 Frivolous Dispute Penalty

Disputes cost attention and lock funds. Overturned disputes produce a negative trust signal on the filing party — equivalent to one failed task in routing weight. An optional filing fee (deducted from the filer's virtual account at `opened`) is refunded if the dispute is upheld or split, forfeited if overturned.

### 9.2 Rate Limiting

Per-agent caps on active disputes prevent a single identity from flooding the system.

### 9.3 Foundation Law

- Overturned disputes must produce a negative trust signal on the filing party. Implementations that skip trust consequences for frivolous disputes are non-conformant.
- Minimum trust threshold for filing is `FirstContact`. Agents below this threshold cannot open disputes.

### 9.4 Convention

- Filing fee: 1% of the disputed allocation amount, refunded on `upheld` or `split`.
- Maximum 3 active disputes per agent.
- Trust penalty for overturned dispute: equivalent to one failed task.

## 10. Security Considerations

**Evidence immutability.** All evidence artifacts are Ed25519-signed. Tampering invalidates the signature and the evidence is rejected. The dispute lifecycle itself is signed at every state transition.

**Relay censorship.** A relay can suppress evidence or refuse to route disputes — but it cannot suppress onchain-anchored proofs. Credential anchors (credential-anchor@1.0) and settlement proofs (settlement@1.0 §7) exist on the public ledger independently of the relay. This is the structural defense for relay-as-defendant disputes (§6.3).

**Collusion.** Federation adjudication requires >50% of the quorum to be compromised for a wrong verdict. The minimum 3-peer quorum and independent voting mitigate collusion. Larger quorums (5, 7) for high-value disputes are recommended but not required by foundation law.

**Privacy.** Dispute details are visible only to the filing party, respondent, and adjudicator(s). The relay must not expose dispute evidence or rationale to uninvolved parties. Federation peers participating in adjudication are bound by the same constraint.

**Timeout exploitation.** Every timeout resolves in a defined direction: `opened` without evidence expires (status quo), arbitration without resolution defaults to `split`, `resolved` without appeal becomes `final`. No timeout leaves funds in limbo.

## 11. Relationship to Other Specs

| Spec                  | Relationship                                                                                                               |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| delegation@1.0        | Defines `BudgetAllocation` with `disputed` state (§6.1). This spec defines the lifecycle that enters and exits that state. |
| execution-ledger@1.0  | `execution_receipt` and `execution_ledger` are primary evidence types (§5.1).                                              |
| credential@1.0        | Credentials serve as evidence. Dispute outcomes may trigger credential revocation for serious violations.                  |
| credential-anchor@1.0 | Onchain anchored credentials are tamper-proof evidence that survives relay censorship (§6.3).                              |
| settlement@1.0        | Settlement proofs are evidence. Fund handling (§7) operates on amounts established by settlement.                          |
| relay-federation@1.0  | Federation peers form the adjudication quorum for cross-relay and relay-as-defendant disputes (§6.2).                      |
| market@1.0            | Virtual account balances are locked during dispute. Resolution credits/debits virtual accounts.                            |
| identity@1.0          | All dispute messages are signed by the party's Ed25519 identity key.                                                       |
| auth-token@1.0        | Dispute API endpoints require signed bearer tokens with appropriate audience binding.                                      |
