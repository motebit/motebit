{
  "$comment": "SPDX-License-Identifier: MIT",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/motebit/motebit/main/spec/schemas/delegation-token-v1.json",
  "title": "DelegationToken (v1)",
  "description": "Signed authorization for a delegate agent to invoke scoped capabilities on behalf of a delegator. Canonicalization: JCS (RFC 8785). Signature: Ed25519 over canonicalJson(body minus signature), base64url-encoded. See spec/market-v1.md §12.1.",
  "type": "object",
  "properties": {
    "delegator_id": {
      "type": "string",
      "minLength": 1,
      "description": "Delegator's motebit identity — UUIDv7 string. The entity authorizing the delegation."
    },
    "delegator_public_key": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Delegator's Ed25519 public key, hex-encoded (64 lowercase characters). Used to verify `signature`. Embedded so verification does not require a relay lookup."
    },
    "delegate_id": {
      "type": "string",
      "minLength": 1,
      "description": "Delegate's motebit identity — UUIDv7 string. The entity receiving the authorization."
    },
    "delegate_public_key": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Delegate's Ed25519 public key, hex-encoded (64 lowercase characters). Binds the delegation to a specific cryptographic identity — a different keypair cannot exercise the token."
    },
    "scope": {
      "type": "string",
      "minLength": 1,
      "description": "Comma-separated capability list the delegate may invoke, or `*` for wildcard. Exhaustive match required at invocation time — absent capabilities are denied. See market-v1 §12.3."
    },
    "issued_at": {
      "type": "number",
      "description": "Unix timestamp in milliseconds when the token was issued."
    },
    "expires_at": {
      "type": "number",
      "description": "Unix timestamp in milliseconds after which the token is invalid. Verifiers reject tokens where `now > expires_at`."
    },
    "suite": {
      "type": "string",
      "const": "motebit-jcs-ed25519-b64-v1",
      "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for delegation tokens today: JCS canonicalization (RFC 8785), Ed25519 signature, base64url-encoded signature, hex-encoded public keys. See @motebit/protocol SUITE_REGISTRY."
    },
    "signature": {
      "type": "string",
      "minLength": 1,
      "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = this object minus `signature`. Verify with `delegator_public_key`."
    }
  },
  "required": [
    "delegator_id",
    "delegator_public_key",
    "delegate_id",
    "delegate_public_key",
    "scope",
    "issued_at",
    "expires_at",
    "suite",
    "signature"
  ],
  "additionalProperties": false,
  "definitions": {
    "DelegationToken": {
      "type": "object",
      "properties": {
        "delegator_id": {
          "type": "string",
          "minLength": 1,
          "description": "Delegator's motebit identity — UUIDv7 string. The entity authorizing the delegation."
        },
        "delegator_public_key": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "Delegator's Ed25519 public key, hex-encoded (64 lowercase characters). Used to verify `signature`. Embedded so verification does not require a relay lookup."
        },
        "delegate_id": {
          "type": "string",
          "minLength": 1,
          "description": "Delegate's motebit identity — UUIDv7 string. The entity receiving the authorization."
        },
        "delegate_public_key": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "Delegate's Ed25519 public key, hex-encoded (64 lowercase characters). Binds the delegation to a specific cryptographic identity — a different keypair cannot exercise the token."
        },
        "scope": {
          "type": "string",
          "minLength": 1,
          "description": "Comma-separated capability list the delegate may invoke, or `*` for wildcard. Exhaustive match required at invocation time — absent capabilities are denied. See market-v1 §12.3."
        },
        "issued_at": {
          "type": "number",
          "description": "Unix timestamp in milliseconds when the token was issued."
        },
        "expires_at": {
          "type": "number",
          "description": "Unix timestamp in milliseconds after which the token is invalid. Verifiers reject tokens where `now > expires_at`."
        },
        "suite": {
          "type": "string",
          "const": "motebit-jcs-ed25519-b64-v1",
          "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for delegation tokens today: JCS canonicalization (RFC 8785), Ed25519 signature, base64url-encoded signature, hex-encoded public keys. See @motebit/protocol SUITE_REGISTRY."
        },
        "signature": {
          "type": "string",
          "minLength": 1,
          "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = this object minus `signature`. Verify with `delegator_public_key`."
        }
      },
      "required": [
        "delegator_id",
        "delegator_public_key",
        "delegate_id",
        "delegate_public_key",
        "scope",
        "issued_at",
        "expires_at",
        "suite",
        "signature"
      ],
      "additionalProperties": false
    }
  }
}
