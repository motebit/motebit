{
  "$comment": "SPDX-License-Identifier: Apache-2.0",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/motebit/motebit/main/spec/schemas/plan-step-failed-payload-v1.json",
  "title": "PlanStepFailedPayload (v1)",
  "description": "Payload of a `plan_step_failed` event — step reached terminal failure. See spec/plan-lifecycle-v1.md §5.4.",
  "type": "object",
  "properties": {
    "plan_id": {
      "type": "string",
      "minLength": 1,
      "description": "UUID of the parent plan."
    },
    "step_id": {
      "type": "string",
      "minLength": 1,
      "description": "UUID of the failed step."
    },
    "ordinal": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "description": "Zero-based position of the step within its plan."
    },
    "error": {
      "type": "string",
      "description": "Error message from the failing step. Consumers MUST NOT parse it semantically."
    },
    "task_id": {
      "description": "Delegation task id; present iff this step was delegated (§3.7).",
      "type": "string"
    },
    "goal_id": {
      "description": "Owning goal when the plan serves a goal.",
      "type": "string"
    }
  },
  "required": ["plan_id", "step_id", "ordinal", "error"],
  "additionalProperties": {}
}
