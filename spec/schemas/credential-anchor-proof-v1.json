{
  "$comment": "SPDX-License-Identifier: Apache-2.0",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/motebit/motebit/main/spec/schemas/credential-anchor-proof-v1.json",
  "title": "CredentialAnchorProof (v1)",
  "description": "Self-verifiable Merkle inclusion proof for one credential within an anchored batch. Carries everything needed to verify without trusting the relay: batch signature + relay public key, Merkle path (siblings + layer sizes + leaf index), and optional chain anchor. A third-party auditor can prove the credential was issued, batched, signed by the relay, and (when anchored) committed onchain — using only this proof + an Ed25519/SHA-256 implementation. See spec/credential-anchor-v1.md.",
  "type": "object",
  "properties": {
    "credential_id": {
      "type": "string",
      "minLength": 1,
      "description": "Identifier of the credential whose inclusion this proof attests."
    },
    "credential_hash": {
      "type": "string",
      "minLength": 1,
      "description": "Hex-encoded SHA-256 hash of the full credential (including the credential's own proof envelope). The leaf value at `leaf_index` of the Merkle tree."
    },
    "batch_id": {
      "type": "string",
      "minLength": 1,
      "description": "Identifier of the CredentialAnchorBatch containing this credential."
    },
    "merkle_root": {
      "type": "string",
      "minLength": 1,
      "description": "Hex-encoded Merkle root of the batch. MUST match the batch's `merkle_root` and the value committed onchain (when `anchor` is non-null). Verifiers compute the root from `credential_hash` + `siblings` + `layer_sizes` and compare."
    },
    "leaf_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of credentials in the batch. Required for batch-signature verification — the canonical signed payload includes this."
    },
    "first_issued_at": {
      "type": "number",
      "description": "Earliest credential timestamp in the batch. Required for batch-signature verification (the canonical payload includes it)."
    },
    "last_issued_at": {
      "type": "number",
      "description": "Latest credential timestamp in the batch. Required for batch-signature verification."
    },
    "leaf_index": {
      "type": "integer",
      "minimum": 0,
      "description": "Position of this credential's leaf in the sorted leaf array (0-indexed). Determines the left/right walk through the Merkle tree."
    },
    "siblings": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Hex-encoded sibling hashes along the Merkle path from leaf to root. Length is roughly log₂(leaf_count). Verifiers fold these with `credential_hash` per the path determined by `leaf_index`."
    },
    "layer_sizes": {
      "type": "array",
      "items": {
        "type": "integer",
        "minimum": 0
      },
      "description": "Number of nodes at each Merkle layer (leaf layer first). Lets verifiers detect odd-leaf promotion (when an odd-sized layer hoists a node unchanged into the next layer) — without this, the Merkle path could be ambiguous at odd boundaries."
    },
    "relay_id": {
      "type": "string",
      "minLength": 1,
      "description": "Motebit identity of the relay that created the batch."
    },
    "relay_public_key": {
      "type": "string",
      "minLength": 1,
      "description": "Hex-encoded Ed25519 public key of the relay. Used to verify `batch_signature` against the canonical batch payload. Embedded in the proof so verification works without a relay-registry lookup."
    },
    "suite": {
      "type": "string",
      "const": "motebit-jcs-ed25519-hex-v1",
      "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-hex-v1` for credential anchors: JCS canonicalization (RFC 8785), Ed25519 primitive, HEX signature encoding (not base64url — distinct from receipts/tokens because anchor proofs interact with onchain submissions where hex is conventional), hex public-key encoding. Verifiers reject missing or unknown values fail-closed."
    },
    "batch_signature": {
      "type": "string",
      "minLength": 1,
      "description": "Hex-encoded Ed25519 signature over `canonicalJson(payload)`. Signed by the relay over the canonical batch payload (NOT this proof — this proof is self-verifiable but unsigned). Carried here so a verifier can confirm the batch is the relay's claimed batch."
    },
    "anchor": {
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "chain": {
              "type": "string",
              "minLength": 1,
              "description": "Chain identifier (e.g. `solana`, `eip155`). Coarse selector for verifier dispatch; the precise network is in `network`."
            },
            "network": {
              "type": "string",
              "minLength": 1,
              "description": "CAIP-2 network identifier (e.g. `solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp` for Solana mainnet). Lets verifiers query the right chain RPC for the transaction."
            },
            "tx_hash": {
              "type": "string",
              "minLength": 1,
              "description": "Transaction hash on the target chain. Verifiers fetch this transaction and confirm its payload contains the batch's Merkle root."
            },
            "anchored_at": {
              "type": "number",
              "description": "Unix timestamp in milliseconds when the chain anchor was confirmed (block-finalized). Lets verifiers reject anchors that confirmed after the credentials they purport to anchor."
            }
          },
          "required": ["chain", "network", "tx_hash", "anchored_at"],
          "additionalProperties": false
        },
        {
          "type": "null"
        }
      ],
      "description": "Onchain anchor metadata, or `null` if the proof's underlying batch was signed but not yet submitted to a chain. Self-verifiable inclusion still works without an onchain anchor (Ed25519 over the Merkle root is sufficient); the chain anchor adds external timestamping that the relay cannot retroactively rewrite."
    }
  },
  "required": [
    "credential_id",
    "credential_hash",
    "batch_id",
    "merkle_root",
    "leaf_count",
    "first_issued_at",
    "last_issued_at",
    "leaf_index",
    "siblings",
    "layer_sizes",
    "relay_id",
    "relay_public_key",
    "suite",
    "batch_signature",
    "anchor"
  ],
  "additionalProperties": true,
  "definitions": {
    "CredentialAnchorProof": {
      "type": "object",
      "properties": {
        "credential_id": {
          "type": "string",
          "minLength": 1,
          "description": "Identifier of the credential whose inclusion this proof attests."
        },
        "credential_hash": {
          "type": "string",
          "minLength": 1,
          "description": "Hex-encoded SHA-256 hash of the full credential (including the credential's own proof envelope). The leaf value at `leaf_index` of the Merkle tree."
        },
        "batch_id": {
          "type": "string",
          "minLength": 1,
          "description": "Identifier of the CredentialAnchorBatch containing this credential."
        },
        "merkle_root": {
          "type": "string",
          "minLength": 1,
          "description": "Hex-encoded Merkle root of the batch. MUST match the batch's `merkle_root` and the value committed onchain (when `anchor` is non-null). Verifiers compute the root from `credential_hash` + `siblings` + `layer_sizes` and compare."
        },
        "leaf_count": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of credentials in the batch. Required for batch-signature verification — the canonical signed payload includes this."
        },
        "first_issued_at": {
          "type": "number",
          "description": "Earliest credential timestamp in the batch. Required for batch-signature verification (the canonical payload includes it)."
        },
        "last_issued_at": {
          "type": "number",
          "description": "Latest credential timestamp in the batch. Required for batch-signature verification."
        },
        "leaf_index": {
          "type": "integer",
          "minimum": 0,
          "description": "Position of this credential's leaf in the sorted leaf array (0-indexed). Determines the left/right walk through the Merkle tree."
        },
        "siblings": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Hex-encoded sibling hashes along the Merkle path from leaf to root. Length is roughly log₂(leaf_count). Verifiers fold these with `credential_hash` per the path determined by `leaf_index`."
        },
        "layer_sizes": {
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 0
          },
          "description": "Number of nodes at each Merkle layer (leaf layer first). Lets verifiers detect odd-leaf promotion (when an odd-sized layer hoists a node unchanged into the next layer) — without this, the Merkle path could be ambiguous at odd boundaries."
        },
        "relay_id": {
          "type": "string",
          "minLength": 1,
          "description": "Motebit identity of the relay that created the batch."
        },
        "relay_public_key": {
          "type": "string",
          "minLength": 1,
          "description": "Hex-encoded Ed25519 public key of the relay. Used to verify `batch_signature` against the canonical batch payload. Embedded in the proof so verification works without a relay-registry lookup."
        },
        "suite": {
          "type": "string",
          "const": "motebit-jcs-ed25519-hex-v1",
          "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-hex-v1` for credential anchors: JCS canonicalization (RFC 8785), Ed25519 primitive, HEX signature encoding (not base64url — distinct from receipts/tokens because anchor proofs interact with onchain submissions where hex is conventional), hex public-key encoding. Verifiers reject missing or unknown values fail-closed."
        },
        "batch_signature": {
          "type": "string",
          "minLength": 1,
          "description": "Hex-encoded Ed25519 signature over `canonicalJson(payload)`. Signed by the relay over the canonical batch payload (NOT this proof — this proof is self-verifiable but unsigned). Carried here so a verifier can confirm the batch is the relay's claimed batch."
        },
        "anchor": {
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "chain": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Chain identifier (e.g. `solana`, `eip155`). Coarse selector for verifier dispatch; the precise network is in `network`."
                },
                "network": {
                  "type": "string",
                  "minLength": 1,
                  "description": "CAIP-2 network identifier (e.g. `solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp` for Solana mainnet). Lets verifiers query the right chain RPC for the transaction."
                },
                "tx_hash": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Transaction hash on the target chain. Verifiers fetch this transaction and confirm its payload contains the batch's Merkle root."
                },
                "anchored_at": {
                  "type": "number",
                  "description": "Unix timestamp in milliseconds when the chain anchor was confirmed (block-finalized). Lets verifiers reject anchors that confirmed after the credentials they purport to anchor."
                }
              },
              "required": ["chain", "network", "tx_hash", "anchored_at"],
              "additionalProperties": false
            },
            {
              "type": "null"
            }
          ],
          "description": "Onchain anchor metadata, or `null` if the proof's underlying batch was signed but not yet submitted to a chain. Self-verifiable inclusion still works without an onchain anchor (Ed25519 over the Merkle root is sufficient); the chain anchor adds external timestamping that the relay cannot retroactively rewrite."
        }
      },
      "required": [
        "credential_id",
        "credential_hash",
        "batch_id",
        "merkle_root",
        "leaf_count",
        "first_issued_at",
        "last_issued_at",
        "leaf_index",
        "siblings",
        "layer_sizes",
        "relay_id",
        "relay_public_key",
        "suite",
        "batch_signature",
        "anchor"
      ],
      "additionalProperties": true
    }
  }
}
