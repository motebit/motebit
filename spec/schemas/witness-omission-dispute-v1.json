{
  "$comment": "SPDX-License-Identifier: Apache-2.0",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/motebit/motebit/main/spec/schemas/witness-omission-dispute-v1.json",
  "title": "WitnessOmissionDispute (v1)",
  "description": "Soft-accountability dispute filed within 24h of `cert.issued_at` by a peer claiming wrongful omission from `cert.witnessed_by[]` on an `append_only_horizon` retention cert. Two evidence shapes (membership proof or alternative peering); adjudicated through the existing DisputeResolution path. Certs remain TERMINAL — a sustained dispute is a reputation hit on the issuer, not a cert invalidation. See docs/doctrine/retention-policy.md decision 5 + 4b-3 sub-notes.",
  "type": "object",
  "properties": {
    "dispute_id": {
      "type": "string",
      "minLength": 1,
      "description": "UUID v7 generated by the disputant. Stable through the existing dispute lifecycle (opened → evidence → arbitration → resolved → final)."
    },
    "cert_issuer": {
      "type": "string",
      "minLength": 1,
      "description": "Motebit identity / operator id of the cert issuer — the relay that signed the disputed `append_only_horizon` cert. The relay reconciles against its local `relay_horizon_certs` table at validation time."
    },
    "cert_signature": {
      "type": "string",
      "minLength": 1,
      "description": "Hex-encoded signature of the disputed cert. Opaque pointer; the relay resolves the cert from its local `relay_horizon_certs` table by signature lookup."
    },
    "disputant_motebit_id": {
      "type": "string",
      "minLength": 1,
      "description": "Motebit identity of the disputant peer claiming wrongful omission from `cert.witnessed_by[]`."
    },
    "evidence": {
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "inclusion_proof",
              "description": "Discriminator. Disputant proves their peer pubkey is committed in `cert.federation_graph_anchor.merkle_root` via the inclusion proof; `witnessed_by[]` then provably omits them."
            },
            "leaf_hash": {
              "type": "string",
              "minLength": 1,
              "description": "Hex-encoded SHA-256 leaf hash for the disputant's federation pubkey under the anchor's canonicalization (lowercase hex pubkey bytes — same recipe as relay-federation-v1.md §7.6 / credential-anchor-v1.md §3)."
            },
            "proof": {
              "type": "object",
              "properties": {
                "siblings": {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "minLength": 1
                  },
                  "description": "Hex-encoded sibling hashes ordered leaf-to-root. Same shape as credential-anchor-v1 §6 and the `inclusion_proof` field on `cert.witnessed_by[i]`."
                },
                "leaf_index": {
                  "type": "integer",
                  "minimum": 0,
                  "description": "0-based index of the disputant's pubkey in the sorted peer set at horizon_ts."
                },
                "layer_sizes": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "minimum": 0
                  },
                  "description": "Per-layer node counts, used to detect odd-leaf promotion when reconstructing the root."
                }
              },
              "required": ["siblings", "leaf_index", "layer_sizes"],
              "additionalProperties": false,
              "description": "Inclusion proof against `cert.federation_graph_anchor.merkle_root`. The `@motebit/crypto :: verifyWitnessOmissionDispute` reconstructs the root and asserts equality with the anchor."
            }
          },
          "required": ["kind", "leaf_hash", "proof"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "alternative_peering",
              "description": "Discriminator. Disputant claims a peering relationship at `cert.horizon_ts` that the issuer's published anchor omitted — i.e., the anchor is incomplete or wrong."
            },
            "peering_artifact": {
              "type": "object",
              "additionalProperties": {},
              "description": "Signed peering artifact issued by the cert issuer, embedding its own signature. Today the canonical shape is a federation Heartbeat (`motebit-concat-ed25519-hex-v1`, payload `${relay_id}|${timestamp}|${suite}`) whose timestamp falls within ±5 min of `cert.horizon_ts` — mirrors `HEARTBEAT_REMOVE_THRESHOLD = 5` × `60s` in services/relay/src/federation.ts. Verifier dispatches on the artifact's self-described shape; future arms (e.g. PeeringConfirm) land as additive registry growth without a wire break."
            }
          },
          "required": ["kind", "peering_artifact"],
          "additionalProperties": false
        }
      ],
      "description": "Exactly one of two evidence shapes — `inclusion_proof` proves membership in the published anchor, `alternative_peering` proves peering attested outside the anchor. The verifier dispatches by `kind`."
    },
    "filed_at": {
      "type": "number",
      "description": "Unix milliseconds when the disputant signed the dispute. The verifier in `@motebit/crypto` enforces TWO clock gates: wall clock vs `cert.issued_at` (load-bearing) AND `filed_at ∈ [cert.issued_at, cert.issued_at + WITNESS_OMISSION_DISPUTE_WINDOW_MS]` (sanity). Disputant-attested timestamps cannot widen the 24h window."
    },
    "suite": {
      "type": "string",
      "const": "motebit-jcs-ed25519-b64-v1",
      "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for phase 4b-3 federation co-witness artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
    },
    "signature": {
      "type": "string",
      "minLength": 1,
      "description": "Base64url-encoded Ed25519 signature by the disputant over `canonicalJson(dispute minus signature)`. Binds every other field — `cert_issuer`, `cert_signature`, `evidence`, `filed_at` — so a sustained dispute can't be re-pointed at a different cert post-hoc."
    }
  },
  "required": [
    "dispute_id",
    "cert_issuer",
    "cert_signature",
    "disputant_motebit_id",
    "evidence",
    "filed_at",
    "suite",
    "signature"
  ],
  "additionalProperties": false,
  "definitions": {
    "WitnessOmissionDispute": {
      "type": "object",
      "properties": {
        "dispute_id": {
          "type": "string",
          "minLength": 1,
          "description": "UUID v7 generated by the disputant. Stable through the existing dispute lifecycle (opened → evidence → arbitration → resolved → final)."
        },
        "cert_issuer": {
          "type": "string",
          "minLength": 1,
          "description": "Motebit identity / operator id of the cert issuer — the relay that signed the disputed `append_only_horizon` cert. The relay reconciles against its local `relay_horizon_certs` table at validation time."
        },
        "cert_signature": {
          "type": "string",
          "minLength": 1,
          "description": "Hex-encoded signature of the disputed cert. Opaque pointer; the relay resolves the cert from its local `relay_horizon_certs` table by signature lookup."
        },
        "disputant_motebit_id": {
          "type": "string",
          "minLength": 1,
          "description": "Motebit identity of the disputant peer claiming wrongful omission from `cert.witnessed_by[]`."
        },
        "evidence": {
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "kind": {
                  "type": "string",
                  "const": "inclusion_proof",
                  "description": "Discriminator. Disputant proves their peer pubkey is committed in `cert.federation_graph_anchor.merkle_root` via the inclusion proof; `witnessed_by[]` then provably omits them."
                },
                "leaf_hash": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Hex-encoded SHA-256 leaf hash for the disputant's federation pubkey under the anchor's canonicalization (lowercase hex pubkey bytes — same recipe as relay-federation-v1.md §7.6 / credential-anchor-v1.md §3)."
                },
                "proof": {
                  "type": "object",
                  "properties": {
                    "siblings": {
                      "type": "array",
                      "items": {
                        "type": "string",
                        "minLength": 1
                      },
                      "description": "Hex-encoded sibling hashes ordered leaf-to-root. Same shape as credential-anchor-v1 §6 and the `inclusion_proof` field on `cert.witnessed_by[i]`."
                    },
                    "leaf_index": {
                      "type": "integer",
                      "minimum": 0,
                      "description": "0-based index of the disputant's pubkey in the sorted peer set at horizon_ts."
                    },
                    "layer_sizes": {
                      "type": "array",
                      "items": {
                        "type": "integer",
                        "minimum": 0
                      },
                      "description": "Per-layer node counts, used to detect odd-leaf promotion when reconstructing the root."
                    }
                  },
                  "required": ["siblings", "leaf_index", "layer_sizes"],
                  "additionalProperties": false,
                  "description": "Inclusion proof against `cert.federation_graph_anchor.merkle_root`. The `@motebit/crypto :: verifyWitnessOmissionDispute` reconstructs the root and asserts equality with the anchor."
                }
              },
              "required": ["kind", "leaf_hash", "proof"],
              "additionalProperties": false
            },
            {
              "type": "object",
              "properties": {
                "kind": {
                  "type": "string",
                  "const": "alternative_peering",
                  "description": "Discriminator. Disputant claims a peering relationship at `cert.horizon_ts` that the issuer's published anchor omitted — i.e., the anchor is incomplete or wrong."
                },
                "peering_artifact": {
                  "type": "object",
                  "additionalProperties": {},
                  "description": "Signed peering artifact issued by the cert issuer, embedding its own signature. Today the canonical shape is a federation Heartbeat (`motebit-concat-ed25519-hex-v1`, payload `${relay_id}|${timestamp}|${suite}`) whose timestamp falls within ±5 min of `cert.horizon_ts` — mirrors `HEARTBEAT_REMOVE_THRESHOLD = 5` × `60s` in services/relay/src/federation.ts. Verifier dispatches on the artifact's self-described shape; future arms (e.g. PeeringConfirm) land as additive registry growth without a wire break."
                }
              },
              "required": ["kind", "peering_artifact"],
              "additionalProperties": false
            }
          ],
          "description": "Exactly one of two evidence shapes — `inclusion_proof` proves membership in the published anchor, `alternative_peering` proves peering attested outside the anchor. The verifier dispatches by `kind`."
        },
        "filed_at": {
          "type": "number",
          "description": "Unix milliseconds when the disputant signed the dispute. The verifier in `@motebit/crypto` enforces TWO clock gates: wall clock vs `cert.issued_at` (load-bearing) AND `filed_at ∈ [cert.issued_at, cert.issued_at + WITNESS_OMISSION_DISPUTE_WINDOW_MS]` (sanity). Disputant-attested timestamps cannot widen the 24h window."
        },
        "suite": {
          "type": "string",
          "const": "motebit-jcs-ed25519-b64-v1",
          "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for phase 4b-3 federation co-witness artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
        },
        "signature": {
          "type": "string",
          "minLength": 1,
          "description": "Base64url-encoded Ed25519 signature by the disputant over `canonicalJson(dispute minus signature)`. Binds every other field — `cert_issuer`, `cert_signature`, `evidence`, `filed_at` — so a sustained dispute can't be re-pointed at a different cert post-hoc."
        }
      },
      "required": [
        "dispute_id",
        "cert_issuer",
        "cert_signature",
        "disputant_motebit_id",
        "evidence",
        "filed_at",
        "suite",
        "signature"
      ],
      "additionalProperties": false
    }
  }
}
