{
  "$comment": "SPDX-License-Identifier: Apache-2.0",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/motebit/motebit/main/spec/schemas/vote-request-v1.json",
  "title": "VoteRequest (v1)",
  "description": "Leader-to-peer fan-out body for §6.2 federation adjudication (relay-federation@1.2 §16). The leader (relay where dispute resolution was requested AND named as filer or respondent) POSTs this to each active federation peer; each peer returns a signed `AdjudicatorVote`.",
  "type": "object",
  "properties": {
    "dispute_id": {
      "type": "string",
      "minLength": 1,
      "description": "The dispute being adjudicated. MUST equal the URL `:disputeId` parameter on the receiving peer's gate ladder."
    },
    "round": {
      "type": "integer",
      "minimum": 1,
      "description": "Adjudication round. 1 for original adjudication; 2 for §8.3 appeal. Signature-bound: cross-round vote replay is cryptographically rejected. The peer's `AdjudicatorVote` response MUST echo this value."
    },
    "dispute_request": {
      "type": "object",
      "properties": {
        "dispute_id": {
          "type": "string",
          "minLength": 1,
          "description": "UUIDv7 generated by the filing party. Stable through the entire dispute lifecycle."
        },
        "task_id": {
          "type": "string",
          "minLength": 1,
          "description": "References the disputed task. Foundation law §4.4: no economic binding (no `task_id` + `allocation_id`), no dispute."
        },
        "allocation_id": {
          "type": "string",
          "minLength": 1,
          "description": "References the task's BudgetAllocation. The dispute mechanism operates on the locked funds; without an allocation there's nothing to release/refund."
        },
        "filed_by": {
          "type": "string",
          "minLength": 1,
          "description": "Motebit identity of the filing party. MUST be a direct party to the referenced task (delegator OR executor) — third parties cannot file."
        },
        "respondent": {
          "type": "string",
          "minLength": 1,
          "description": "Motebit identity of the other task party. Receives notification + evidence window."
        },
        "category": {
          "type": "string",
          "enum": ["quality", "non_payment", "receipt_invalid", "unauthorized", "other"],
          "description": "Dispute category (§4.2). `quality` = work was unsatisfactory; `non_payment` = settlement didn't happen; `receipt_invalid` = receipt fails verification; `unauthorized` = work outside the scoped delegation; `other` = catch-all (description should explain)."
        },
        "description": {
          "type": "string",
          "description": "Human-readable explanation. Empty string is permitted (the evidence speaks for itself), but conventional usage includes a short factual narrative."
        },
        "evidence_refs": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "minItems": 1,
          "description": "References to signed artifacts supporting the dispute. At least one is required at filing time (foundation law §4.4) — disputes without evidence are noise."
        },
        "filed_at": {
          "type": "number",
          "description": "Unix timestamp in milliseconds when the filing party signed the request."
        },
        "suite": {
          "type": "string",
          "const": "motebit-jcs-ed25519-b64-v1",
          "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for dispute artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
        },
        "signature": {
          "type": "string",
          "minLength": 1,
          "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = the artifact minus `signature`. Signed by the filing party."
        }
      },
      "required": [
        "dispute_id",
        "task_id",
        "allocation_id",
        "filed_by",
        "respondent",
        "category",
        "description",
        "evidence_refs",
        "filed_at",
        "suite",
        "signature"
      ],
      "additionalProperties": false,
      "description": "Original signed dispute artifact (§4.2). The receiving peer can re-verify the dispute's provenance from this alone — no separate authentication of the dispute is required."
    },
    "evidence_bundle": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "dispute_id": {
            "type": "string",
            "minLength": 1,
            "description": "References an open dispute. Evidence submitted to a non-existent or non-open dispute is rejected."
          },
          "submitted_by": {
            "type": "string",
            "minLength": 1,
            "description": "Motebit identity of the submitting party. MUST be one of the dispute's parties; third-party evidence routes through one of them."
          },
          "evidence_type": {
            "type": "string",
            "enum": [
              "execution_receipt",
              "credential",
              "anchor_proof",
              "settlement_proof",
              "execution_ledger",
              "attestation"
            ],
            "description": "Evidence type (§5.1). Each value names a category of cryptographically-verifiable artifact the adjudicator can validate against its own dedicated schema."
          },
          "evidence_data": {
            "type": "object",
            "additionalProperties": {},
            "description": "The signed artifact itself, as a JSON object. Inner shape validates against the dedicated wire schema for `evidence_type` (e.g. ExecutionReceiptSchema for `execution_receipt`). Per-entry validation deferred to those schemas."
          },
          "description": {
            "type": "string",
            "description": "Human-readable explanation of what this evidence demonstrates. Lets adjudicators quickly orient on the submitter's claim."
          },
          "submitted_at": {
            "type": "number",
            "description": "Unix timestamp in milliseconds when the evidence was signed."
          },
          "suite": {
            "type": "string",
            "const": "motebit-jcs-ed25519-b64-v1",
            "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for dispute artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
          },
          "signature": {
            "type": "string",
            "minLength": 1,
            "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = the artifact minus `signature`. Signed by the submitting party."
          }
        },
        "required": [
          "dispute_id",
          "submitted_by",
          "evidence_type",
          "evidence_data",
          "description",
          "submitted_at",
          "suite",
          "signature"
        ],
        "additionalProperties": false
      },
      "description": "All evidence collected during the dispute's evidence window (§5.2). For round=2 (appeal), MUST carry the original round-1 evidence plus any new evidence introduced with the appeal per §8.4."
    },
    "requester_id": {
      "type": "string",
      "minLength": 1,
      "description": "Leader relay's `motebit_id`. MUST be a known peer to the receiver (gate 2). MUST match the body's `requester_id` against the resolved peer row (gate 3) so a malicious peer cannot impersonate another relay's vote-request."
    },
    "requested_at": {
      "type": "number",
      "description": "Unix timestamp in milliseconds when the leader signed. Used by the peer-side gate-5 freshness check — requests where `now - requested_at > FEDERATION_VOTE_REQUEST_MAX_AGE_MS` (default 60000ms) are rejected with 400 `request_stale`."
    },
    "suite": {
      "type": "string",
      "const": "motebit-jcs-ed25519-b64-v1",
      "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for dispute artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
    },
    "signature": {
      "type": "string",
      "minLength": 1,
      "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = the artifact minus `signature`. Signed by the leader (requester) over canonical JSON of all fields except signature. The signature binds `dispute_id`, `round`, `requester_id`, and the evidence bundle so request-tampering and cross-round replay both fail-closed."
    }
  },
  "required": [
    "dispute_id",
    "round",
    "dispute_request",
    "evidence_bundle",
    "requester_id",
    "requested_at",
    "suite",
    "signature"
  ],
  "additionalProperties": false,
  "definitions": {
    "VoteRequest": {
      "type": "object",
      "properties": {
        "dispute_id": {
          "type": "string",
          "minLength": 1,
          "description": "The dispute being adjudicated. MUST equal the URL `:disputeId` parameter on the receiving peer's gate ladder."
        },
        "round": {
          "type": "integer",
          "minimum": 1,
          "description": "Adjudication round. 1 for original adjudication; 2 for §8.3 appeal. Signature-bound: cross-round vote replay is cryptographically rejected. The peer's `AdjudicatorVote` response MUST echo this value."
        },
        "dispute_request": {
          "type": "object",
          "properties": {
            "dispute_id": {
              "type": "string",
              "minLength": 1,
              "description": "UUIDv7 generated by the filing party. Stable through the entire dispute lifecycle."
            },
            "task_id": {
              "type": "string",
              "minLength": 1,
              "description": "References the disputed task. Foundation law §4.4: no economic binding (no `task_id` + `allocation_id`), no dispute."
            },
            "allocation_id": {
              "type": "string",
              "minLength": 1,
              "description": "References the task's BudgetAllocation. The dispute mechanism operates on the locked funds; without an allocation there's nothing to release/refund."
            },
            "filed_by": {
              "type": "string",
              "minLength": 1,
              "description": "Motebit identity of the filing party. MUST be a direct party to the referenced task (delegator OR executor) — third parties cannot file."
            },
            "respondent": {
              "type": "string",
              "minLength": 1,
              "description": "Motebit identity of the other task party. Receives notification + evidence window."
            },
            "category": {
              "type": "string",
              "enum": ["quality", "non_payment", "receipt_invalid", "unauthorized", "other"],
              "description": "Dispute category (§4.2). `quality` = work was unsatisfactory; `non_payment` = settlement didn't happen; `receipt_invalid` = receipt fails verification; `unauthorized` = work outside the scoped delegation; `other` = catch-all (description should explain)."
            },
            "description": {
              "type": "string",
              "description": "Human-readable explanation. Empty string is permitted (the evidence speaks for itself), but conventional usage includes a short factual narrative."
            },
            "evidence_refs": {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              },
              "minItems": 1,
              "description": "References to signed artifacts supporting the dispute. At least one is required at filing time (foundation law §4.4) — disputes without evidence are noise."
            },
            "filed_at": {
              "type": "number",
              "description": "Unix timestamp in milliseconds when the filing party signed the request."
            },
            "suite": {
              "type": "string",
              "const": "motebit-jcs-ed25519-b64-v1",
              "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for dispute artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
            },
            "signature": {
              "type": "string",
              "minLength": 1,
              "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = the artifact minus `signature`. Signed by the filing party."
            }
          },
          "required": [
            "dispute_id",
            "task_id",
            "allocation_id",
            "filed_by",
            "respondent",
            "category",
            "description",
            "evidence_refs",
            "filed_at",
            "suite",
            "signature"
          ],
          "additionalProperties": false,
          "description": "Original signed dispute artifact (§4.2). The receiving peer can re-verify the dispute's provenance from this alone — no separate authentication of the dispute is required."
        },
        "evidence_bundle": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "dispute_id": {
                "type": "string",
                "minLength": 1,
                "description": "References an open dispute. Evidence submitted to a non-existent or non-open dispute is rejected."
              },
              "submitted_by": {
                "type": "string",
                "minLength": 1,
                "description": "Motebit identity of the submitting party. MUST be one of the dispute's parties; third-party evidence routes through one of them."
              },
              "evidence_type": {
                "type": "string",
                "enum": [
                  "execution_receipt",
                  "credential",
                  "anchor_proof",
                  "settlement_proof",
                  "execution_ledger",
                  "attestation"
                ],
                "description": "Evidence type (§5.1). Each value names a category of cryptographically-verifiable artifact the adjudicator can validate against its own dedicated schema."
              },
              "evidence_data": {
                "type": "object",
                "additionalProperties": {},
                "description": "The signed artifact itself, as a JSON object. Inner shape validates against the dedicated wire schema for `evidence_type` (e.g. ExecutionReceiptSchema for `execution_receipt`). Per-entry validation deferred to those schemas."
              },
              "description": {
                "type": "string",
                "description": "Human-readable explanation of what this evidence demonstrates. Lets adjudicators quickly orient on the submitter's claim."
              },
              "submitted_at": {
                "type": "number",
                "description": "Unix timestamp in milliseconds when the evidence was signed."
              },
              "suite": {
                "type": "string",
                "const": "motebit-jcs-ed25519-b64-v1",
                "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for dispute artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
              },
              "signature": {
                "type": "string",
                "minLength": 1,
                "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = the artifact minus `signature`. Signed by the submitting party."
              }
            },
            "required": [
              "dispute_id",
              "submitted_by",
              "evidence_type",
              "evidence_data",
              "description",
              "submitted_at",
              "suite",
              "signature"
            ],
            "additionalProperties": false
          },
          "description": "All evidence collected during the dispute's evidence window (§5.2). For round=2 (appeal), MUST carry the original round-1 evidence plus any new evidence introduced with the appeal per §8.4."
        },
        "requester_id": {
          "type": "string",
          "minLength": 1,
          "description": "Leader relay's `motebit_id`. MUST be a known peer to the receiver (gate 2). MUST match the body's `requester_id` against the resolved peer row (gate 3) so a malicious peer cannot impersonate another relay's vote-request."
        },
        "requested_at": {
          "type": "number",
          "description": "Unix timestamp in milliseconds when the leader signed. Used by the peer-side gate-5 freshness check — requests where `now - requested_at > FEDERATION_VOTE_REQUEST_MAX_AGE_MS` (default 60000ms) are rejected with 400 `request_stale`."
        },
        "suite": {
          "type": "string",
          "const": "motebit-jcs-ed25519-b64-v1",
          "description": "Cryptosuite identifier. Always `motebit-jcs-ed25519-b64-v1` for dispute artifacts: JCS canonicalization (RFC 8785), Ed25519 primitive, base64url signature, hex public key. Verifiers reject missing or unknown values fail-closed."
        },
        "signature": {
          "type": "string",
          "minLength": 1,
          "description": "Base64url-encoded Ed25519 signature over `canonicalJson(body)` where `body` = the artifact minus `signature`. Signed by the leader (requester) over canonical JSON of all fields except signature. The signature binds `dispute_id`, `round`, `requester_id`, and the evidence bundle so request-tampering and cross-round replay both fail-closed."
        }
      },
      "required": [
        "dispute_id",
        "round",
        "dispute_request",
        "evidence_bundle",
        "requester_id",
        "requested_at",
        "suite",
        "signature"
      ],
      "additionalProperties": false
    }
  }
}
