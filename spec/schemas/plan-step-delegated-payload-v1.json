{
  "$comment": "SPDX-License-Identifier: Apache-2.0",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/motebit/motebit/main/spec/schemas/plan-step-delegated-payload-v1.json",
  "title": "PlanStepDelegatedPayload (v1)",
  "description": "Payload of a `plan_step_delegated` event — step handed off to a remote agent. See spec/plan-lifecycle-v1.md §5.5.",
  "type": "object",
  "properties": {
    "plan_id": {
      "type": "string",
      "minLength": 1,
      "description": "UUID of the parent plan."
    },
    "step_id": {
      "type": "string",
      "minLength": 1,
      "description": "UUID of the delegated step."
    },
    "ordinal": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "description": "Zero-based position of the step within its plan."
    },
    "task_id": {
      "type": "string",
      "minLength": 1,
      "description": "Relay-issued task identifier. Matches the subsequent AgentTaskCompleted.task_id."
    },
    "routing_choice": {
      "description": "Routing provenance picked by the semiring. Opaque to this spec; forward-compat via passthrough.",
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {}
    },
    "goal_id": {
      "description": "Owning goal when the plan serves a goal.",
      "type": "string"
    }
  },
  "required": ["plan_id", "step_id", "ordinal", "task_id"],
  "additionalProperties": {}
}
