{
  "$comment": "SPDX-License-Identifier: Apache-2.0",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/motebit/motebit/main/spec/schemas/memory-accessed-payload-v1.json",
  "title": "MemoryAccessedPayload (v1)",
  "description": "Payload of a `memory_accessed` event — emitted when a live node is read for recall, reflection, or consolidation. See spec/memory-delta-v1.md §5.2.",
  "type": "object",
  "properties": {
    "node_id": {
      "type": "string",
      "minLength": 1,
      "description": "UUID of the accessed node."
    }
  },
  "required": ["node_id"],
  "additionalProperties": true,
  "definitions": {
    "MemoryAccessedPayload": {
      "type": "object",
      "properties": {
        "node_id": {
          "type": "string",
          "minLength": 1,
          "description": "UUID of the accessed node."
        }
      },
      "required": ["node_id"],
      "additionalProperties": true
    }
  }
}
