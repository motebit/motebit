---
---

Deliberate no-release marker: prettier 3.8.3 → 3.9.x (dev-minor-patch group,
#309) reformats 81 files, including published-package sources (protocol, sdk,
verify, state-export-client, cli). Audited as formatting-only — no semantic
change, nothing that alters published dist behavior, so no version bump is
warranted.
