---
"@motebit/wallet-solana": minor
"@motebit/relay": patch
---

Solana-side treasury reconciliation lands as a sibling to the EVM x402 fee-collection reconciler. Closes the audit asymmetry that opened when Arc 2 of the off-ramp arc landed atomic multi-output P2P settlement (delegator→worker + delegator→treasury fee legs in one Solana tx, recorded against the relay's identity-derived Solana treasury wallet) but left no automated cycle comparing the recorded `platform_fee` accumulation against the wallet's onchain USDC balance.

**Algebra — `@motebit/wallet-solana`.** New export `OperatorSolanaTreasuryReconciler` (sibling of `OperatorSolanaTransfer`, same package, same construction pattern). Pure orchestration class — store + adapter injection, no I/O. Mirrors the EVM `reconcileTreasury` algebra by structural agreement, not code reuse: the wire-level dependency differs (EVM uses `EvmRpcAdapter.getBalance({contractAddress, accountAddress})`; Solana uses `SolanaRpcAdapter.getUsdcBalance()` against the adapter's construction-site USDC mint), so the two reconcilers live next to their chain-specific adapters per the precedent set by deposit detection (EVM in `@motebit/deposit-detector`, Solana inside `@motebit/wallet-solana`). New public surface: `OperatorSolanaTreasuryReconciler` class, `createOperatorSolanaTreasuryReconciler` factory, `SolanaReconciliationResult` / `SolanaTreasuryReconciliationStore` / `SolanaTreasuryReconciliationLogger` interfaces, `SOLANA_TREASURY_DEFAULT_CHAIN` constant (re-exports `SOLANA_MAINNET_CAIP2` = `"solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp"` per CAIP-30; one canonical source of truth in `memo-submitter.ts`), `SOLANA_DEFAULT_CONFIRMATION_LAG_BUFFER_MS` constant (5 min).

**Relay-side wiring — `services/relay/src/solana-treasury-reconciliation.ts`.** `SqliteSolanaTreasuryReconciliationStore` implements the package's store interface against the existing `relay_treasury_reconciliations` table — schema unchanged, the `chain` column carries canonical CAIP-2 strings per CAIP-30 and discriminates rows (`eip155:8453` for EVM-Base, `solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp` for Solana mainnet; same identifier as the credential-anchor + consolidation-receipt schemas in `spec/`). One table, two writers, one admin endpoint. The recorded-fee-sum query filters `settlement_mode = 'p2p' AND payment_verification_status = 'verified' AND settled_at < asOfMs`. The verified-only filter is load-bearing: P2P settlements record `platform_fee` at submission but the funds only reach the treasury after `p2p-verifier.ts` confirms both legs landed onchain (Arc 2 atomic multi-output tx) — counting pending or failed rows would produce false-positive negative drift. `startSolanaTreasuryReconciliationLoop` mirrors the EVM loop's shape (15 min cadence, configurable via `MOTEBIT_SOLANA_TREASURY_RECONCILIATION_INTERVAL_MS`; respects `isFrozen()` emergency-freeze callback).

**Boot wiring.** The Solana reconciliation loop fires inside the existing `if (solanaRpcUrl)` block in `services/relay/src/index.ts`, paired with `startP2pVerifierLoop` — verifier produces the rows, reconciler audits them. Disabled with `solana-treasury-reconciliation.disabled` log line when `SOLANA_RPC_URL` is unset. `clearInterval` registered for graceful shutdown.

**Admin endpoint reshape.** `GET /api/v1/admin/treasury-reconciliation` response shape changed from the prior flat `{stats, records, treasury_address, chain, loop_enabled}` to per-chain `{chains: [{chain, treasury_address, usdc_contract, loop_enabled, stats}, ...], records}`. Records list stays flat, sortable cross-chain by `run_at desc`. Master-token gating unchanged. No external consumer of this admin endpoint exists yet (admin-only), so the break is internal. `getTreasuryReconciliationStats(db, chain?)` extended with an optional chain filter; chain-less call preserved for callers that pre-date the multi-chain reshape.

**Transparency declaration.** `services/relay/src/transparency.ts` `DECLARATION_CONTENT` gains a clause describing the Solana reconciliation cycles' append-only audit log. The transparency sibling-boundary test (`services/relay/src/__tests__/transparency.test.ts`) enforces declaration⇔code sync; `PRIVACY.md` regenerated from the renderer.

**Drift gate #104 — `check-solana-treasury-reconciliation`.** When `services/relay/src/index.ts` wires `startP2pVerifierLoop`, the same file must also wire `startSolanaTreasuryReconciliationLoop`. Reverse direction also enforced: an isolated reconciler with no verifier is a configuration mistake. Same paired-presence shape as `check-deposit-detector-confirmations` (#72): two primitives that must travel together because they share a structural commitment. Perturbation probe registered with `check-gates-effective`. Drift-defenses inventory entry added.

**Doctrine — `docs/doctrine/treasury-custody.md`.** New section "Solana p2p-fee reconciliation" extending the existing two-phase doctrine. Names the structural asymmetry vs EVM (EVM x402 facilitator settles synchronously; Solana P2P is async-verified, drift detects three failure modes structurally absent on EVM), the one-table-two-writers shape (`relay_treasury_reconciliations` discriminated by `chain` column), and the drift gate. `services/relay/CLAUDE.md` rule 16 extended to cover both EVM and Solana sides.

**Tests.** `packages/wallet-solana/src/__tests__/operator-treasury-reconciler.test.ts` (8 tests — fake adapter + in-memory store, covers consistent / drift / RPC-error-no-persist / store-error-no-persist / confirmation-lag exclusion / ID injection / chain override). `services/relay/src/__tests__/solana-treasury-reconciliation.test.ts` (10 tests — SqliteStore against a real relay DB, covers verified-only filter / pending-excluded / failed-excluded / EVM-row-excluded / confirmation-lag exclusion / persistence round-trip / coexistence with EVM rows in the same table / loop respects isFrozen / loop persists results). `services/relay/src/__tests__/treasury-reconciliation-e2e.test.ts` updated to the new per-chain response shape.
