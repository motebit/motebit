---
"@motebit/runtime": patch
---

**Third slice of PR 1 of the agent-surface pivot — cobrowse-as-mode reshape (runtime + web half).** Closes the polarity-asymmetry on the user-state side. The doctrine renames the cobrowser-shaped chrome from "the persistent default" to "an entered mode," but until this slice the user-state chrome carried no explicit indicator that it was a MODE, and there was no protocol-level path for the user to YIELD drive back to motebit (only motebit-side `releaseControl` existed). Both halves close here. The protocol counterpart (the `yield_control` transition kind on `CO_BROWSE_TRANSITION_KINDS`) ships in the published sibling `.changeset/slab-chrome-cobrowse-as-mode.md`.

**Runtime — `yieldControl(by: "user")`.** New method on `CoBrowseControlMachine` paired with the protocol's `yield_control` transition kind. Goes from `user` → `motebit` with no approval needed (user's identity is the trust root; the user unilaterally decides who drives, including handing back).

**Surface affordance — chip-button, single mechanism.** The user-state chrome now includes a `"motebit waiting"` chip in the trail (after the ←/→/↻ history buttons). Promoted from a static label to a `<button type="button">` so click semantics, focus-ring, and keyboard activation come from the platform; `aria-label="Hand back to motebit — motebit is watching and will resume driving"` carries the affordance's full intent for assistive tech. Calm-default visual treatment (borderless tinted text, transparent background, hover lifts opacity) inherits the chrome's history-button family. Click dispatches `motebit:cobrowse-back` — the same CustomEvent the `/back` slash command uses. Web-app handler calls `machine.yieldControl("user")` directly. Surface-determinism preserved: typed CustomEvent → typed-capability call, no AI-loop routing; `check-affordance-routing` approves by construction.

**The full PR 1 lifecycle now reachable from any surface.** Motebit-default register renders; user invokes `/wheel` or taps the URL chip → state flips to user + URL bar focuses (the prior slice's gotcha closure); user navigates / clicks / types in the page; user invokes `/back` or taps the "motebit waiting" chip → state flips back, motebit resumes the narration register naturally. Single mode-flip mechanism in each direction; the slash command and the chip are surface affordances that converge on the same wire event. Future polish (gestures, voice, explicit Take-the-Wheel button) compose against the same events.

**Tests.** 4 new tests in `packages/runtime/src/__tests__/co-browse-control.test.ts`: `yieldControl(user)` from user state transitions to motebit with the right audit payload; rejects from non-user states with `invalid_from_state`; rejects from non-user party with `wrong_party`; yield + reclaim are symmetric (user can flip back and forth without state corruption). 3 new/updated tests in `apps/web/src/__tests__/cobrowse-chrome.test.ts`: user-state structure now asserts the chip's presence + content; motebit-state asserts chip's absence (polarity guard); chip-click dispatches `motebit:cobrowse-back`; chip is a `<button>` with `aria-label`. Existing tests (state-keyed structure, surface-determinism, etc.) unchanged. 483/483 web tests pass; 986/986 runtime tests pass.

Closes PR 1's full scope: state-driven chrome dispatcher (slice 1), motebit×virtual_browser register + narration wire (slice 1), `/wheel` + chip-tap-from-motebit-register + URL-bar focus surfacing (slice 2), `yield_control` protocol + `/back` + "motebit waiting" chip + chip-tap-from-user-register (this slice). Other embodiment columns stay deferred to PR N. The pivot is structural in the codebase end-to-end.
