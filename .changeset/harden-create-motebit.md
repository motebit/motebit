---
"create-motebit": patch
---

Harden build: switch to tsup for robust shebang injection, inject version strings at build time to eliminate drift, bundle all dependencies into single zero-dep output.
