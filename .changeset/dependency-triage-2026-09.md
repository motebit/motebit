---
"motebit": patch
---

Security dependency triage: `hono` ^4.13.5 (query-fragment and `parseBody` nesting fixes); transitive `ws`, `fast-uri`, `qs`, `uuid` patched via upper-bounded overrides. Full ledger with reachability and lift triggers in `docs/security/dependency-triage.md`.
